<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Results</title>
    <style>
        body{
            font-family: sans-serif;
            background-color: #FFFAFA;
        }
        table{
            border-collapse: collapse;
            margin: auto;
        }
        table,th,td{
            border: 1px solid black;
            padding:5px
        }
        th {
            text-align: left;
            height:50px;
        }
        h2,h3,h4{
            text-align: center;
        }
    </style>
</head>
<body>
    <h2>Results for "<%= queries.query %>"  in <%= queries.centre %>, <%= queries.stream %></h2>
    <%if(response.length!=0) { %>
    <table style="width:50%">
        <tr>
          <th>Name</th>
          <th>Roll no</th>
        </tr>
        <% response.forEach((resp) => { %>
        <tr>
          <td><%= resp.name %></td>
          <td><%= resp.roll %></td>
        </tr>
        <% }) %>
      </table>
      <h2>Search using roll no. on <a href="https://www.results.shiksha/goa/hssc.htm" target="_blank">results.shiksha</a></h2>

      <%}else{%>
        <h3>No results found, if you think this is an error then please make sure to follow the instructions on the home page</h3>
      <%}%>
    <h4><a href="/">Go back</a></h4>
    <h3>Developed by <a href="https://kartiksawant.github.io" target="_blank">Kartik Sawant</a></h3>
</body>
</html>